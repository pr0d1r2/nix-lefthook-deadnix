# Changelog

All notable changes to this project are documented here.

## Unreleased

- Use the standard `confirm` app instead of a local copy that omitted the
  auto-detected `actions` and `bats` fragment wrappers.
